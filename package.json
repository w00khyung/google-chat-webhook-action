{
  "name": "google-chat-webhook-action",
  "version": "1.0.0",
  "description": "Send GitHub Actions notifications to Google Chat using cardsV2 format",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/**/*.js",
    "package": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/w00khyung/google-chat-webhook-action.git"
  },
  "keywords": [
    "github",
    "action",
    "google-chat",
    "webhook",
    "notification"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  }
}
